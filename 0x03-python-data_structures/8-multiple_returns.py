#!/usr/bin/python3
def multiple_returns(sentence):
    """Function returns a tuple with lenght of a string and first character."""
    length = len(sentence)
    if lenth > 0:
        string = lenght, sentence[0]
    else:
        string = lenght, None
    return string
